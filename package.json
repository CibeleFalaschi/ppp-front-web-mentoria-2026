{
  "name": "clientes-crm-web",
  "version": "1.0.0",
  "description": "Aplicação web para gestão de clientes consumindo a API REST local.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "cypress run",
    "cy:headed": "cypress run --headed",
    "cy:open": "cypress open"
  },
  "dependencies": {
    "express": "^4.19.2"
  },
  "devDependencies": {
    "cypress": "^15.20.0"
  }
}
